const menuButton=document.querySelector('.menu-toggle');const mobileMenu=document.querySelector('.mobile-menu');if(menuButton&&mobileMenu){menuButton.addEventListener('click',()=>{const open=mobileMenu.classList.toggle('is-open');menuButton.setAttribute('aria-expanded',String(open));menuButton.setAttribute('aria-label',open?'Close menu':'Open menu')});}
document.querySelector('.newsletter form')?.addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget;const button=form.querySelector('button');const email=form.querySelector('input[type=email]').value;button.disabled=true;button.textContent='Signing Up...';try{const response=await fetch('api/newsletter',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,source:'homepage'})});const result=await response.json();if(!response.ok)throw new Error(result.error||'Unable to subscribe');button.textContent='Thank You';form.reset()}catch(error){button.textContent='Try Again';button.title=error.message}finally{button.disabled=false}});
