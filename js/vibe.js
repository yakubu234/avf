const vibeTabs=[...document.querySelectorAll('[data-vibe-tab]')];const vibePanels=[...document.querySelectorAll('[data-vibe-panel]')];vibeTabs.forEach(tab=>tab.addEventListener('click',()=>{const target=tab.dataset.vibeTab;vibeTabs.forEach(item=>{const active=item===tab;item.classList.toggle('active',active);item.setAttribute('aria-selected',String(active))});vibePanels.forEach(panel=>{const active=panel.dataset.vibePanel===target;panel.classList.toggle('active',active);panel.hidden=!active})}));document.querySelector('.vibe-signup form')?.addEventListener('submit',event=>{event.preventDefault();event.currentTarget.querySelector('button').textContent='Thank You';event.currentTarget.reset()});
